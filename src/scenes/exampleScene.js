class ExampleScene {
    constructor() {
      // Initialize scene objects here (e.g., create models, load textures)
    }
  
    render(gl) {
      // Render each object in the scene here
    }
  }
  
  export default ExampleScene;